import { Injectable } from '@angular/core';
import {Place } from '../objects/place';

@Injectable()
export class RankService {
    public sendReview(stars: number, textReview: string): void{
        //TODO: send the review to the server
    }    
 } 
    